import org.junit.Test;

public class TestBeanTest {

    @Test
    public void testServer() throws InterruptedException {
        /*
        ClientAndServer server = new ClientAndServer();
        server.startClientAndServer(3003);

        MockServerClient mockServerClient = new MockServerClient("localhost", 3003);
        String body = "Hello World";
        mockServerClient.when(HttpRequest.request("/api/user")).respond(HttpResponse.response().withStatusCode(200).withBody(body));

        Thread.sleep(20000);
        mockServerClient.verifyZeroInteractions();

         */
    }

}
